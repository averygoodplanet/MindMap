extends ../template/layout
include ../mixins/form-helper

block content
  .row
    .small-12.columns
      h1 Table Page
      a.button.small(href="/new") New MindMap
      h5 later Open buttons will go to Edit Page
  .row
    .small-12.columns
      for map in maps
        p= map.title
        p= map.lastModified
        p= map._id